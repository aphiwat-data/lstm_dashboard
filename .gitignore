# Python
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# AWS credentials — never commit these
.aws/
*.pem
credentials
config

# Local data / model artifacts pulled from S3 (source of truth is S3, not git)
*.npy
*.pkl
*.keras
*.parquet

# Hourly pipeline raw/processed data (re-creatable from Dukascopy; keep out of git)
hourly/data/
daily/data/
hourly/node_modules/
hourly/package-lock.json

# OS / editor cruft
.DS_Store
.vscode/
